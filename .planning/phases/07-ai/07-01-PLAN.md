---
phase: 07-ai
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/styles/style-service.ts
  - src/lib/workers/handlers/style-extract-task-handler.ts
  - src/lib/workers/index.ts
  - src/app/api/user-styles/[id]/upload-ref/route.ts
  - tests/unit/style-extract.test.ts
autonomous: true
requirements:
  - EXTRACT-01
  - EXTRACT-02
  - EXTRACT-03
  - EXTRACT-04

must_haves:
  truths:
    - "用户可以上传图片文件，图片存储到 S3 路径 user-styles/{userId}/{styleId}/ref.jpg"
    - "上传后系统调用 LLM Vision，返回的提示词描述风格特征而非图片内容"
    - "AI 提取状态有三态追踪：pending、completed、failed"
    - "用户可以在 completed 状态下编辑 AI 生成的草稿后保存"
    - "提取失败时用户看到明确错误提示，可重试"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "extractionStatus 和 extractionMessage 字段"
      contains: "extractionStatus"
    - path: "src/lib/workers/handlers/style-extract-task-handler.ts"
      provides: "异步提取 Worker"
      exports: "styleExtractTaskHandler"
    - path: "src/app/api/user-styles/[id]/upload-ref/route.ts"
      provides: "图片上传 API"
      exports: "POST"

---

<objective>
实现 AI 参考图提取功能：用户上传参考图 → S3 存储 → 异步 LLM Vision 提取风格描述 → 三态追踪 → 可编辑保存。

**Purpose:** 用户通过上传参考图让 AI 自动提取风格特征，加速风格创建流程
**Output:** 图片上传 API + 异步提取 Worker + Schema 迁移 + 单元测试
</objective>

<context>
@src/lib/llm/vision.ts
@src/lib/storage/index.ts
@src/lib/workers/index.ts
@prisma/schema.prisma
@.planning/phases/07-ai/07-CONTEXT.md

# LLM Vision API (from vision.ts)
chatCompletionWithVision(userId, model, textPrompt, imageUrls[], options)

# Storage API (from storage/index.ts)
uploadObject(body, key, contentType) → key
generateUniqueKey(prefix, ext) → key

# Worker Pattern (from workers/index.ts)
Queue('style-extract', { connection })
worker.on('completed', job => ...)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema 迁移 — 新增 extractionStatus 和 extractionMessage 字段</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (UserStyle 模型当前定义)
    - .planning/phases/01-userstyle/01-CONTEXT.md (D-01~D-04 原始 schema 决策)
  </read_first>
  <action>
在 `prisma/schema.prisma` 的 `UserStyle` 模型中添加两个新字段：

```prisma
model UserStyle {
  id              String   @id @default(uuid())
  userId          String
  name            String
  promptZh        String   @db.Text
  promptEn        String   @db.Text
  tags            String?
  referenceImageUrl String?
  isSystem        Boolean  @default(false)
  extractionStatus  String  @default("pending")  // 新增: pending | completed | failed
  extractionMessage String?                       // 新增: 失败时的错误信息
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  user            User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, name])
  @@index([userId])
  @@map("user_styles")
}
```

然后执行迁移：
```bash
npx prisma migrate dev --name add_extraction_status
```
  </action>
  <verify>
    <automated>cd "/Users/gaoziwen/vibe coding/导演终结者" && grep -n "extractionStatus" prisma/schema.prisma | head -5</automated>
  </verify>
  <done>
    prisma/schema.prisma 包含 extractionStatus 和 extractionMessage 字段
    npx prisma migrate dev 成功
    npx prisma generate 成功
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建图片上传 API — POST /api/user-styles/:id/upload-ref</name>
  <files>src/app/api/user-styles/[id]/upload-ref/route.ts</files>
  <read_first>
    - src/app/api/user-styles/[id]/route.ts (现有 API 模式)
    - src/lib/storage/index.ts (uploadObject, generateUniqueKey)
    - src/lib/styles/style-service.ts (updateUserStyle)
    - src/lib/api-auth.ts (requireUserAuth)
  </read_first>
  <action>
创建 `src/app/api/user-styles/[id]/upload-ref/route.ts`：

```typescript
/**
 * 用户风格参考图上传 API
 * POST /api/user-styles/:id/upload-ref
 */

import { NextRequest, NextResponse } from 'next/server'
import { requireUserAuth, isErrorResponse } from '@/lib/api-auth'
import { ApiError, apiHandler } from '@/lib/api-errors'
import { uploadObject, generateUniqueKey } from '@/lib/storage'
import { prisma } from '@/lib/prisma'

type RouteParams = { params: Promise<{ id: string }> }

// POST /api/user-styles/:id/upload-ref - 上传参考图
export const POST = apiHandler(async (request: NextRequest, ctx: RouteParams) => {
  // 1. 认证
  const authResult = await requireUserAuth()
  if (isErrorResponse(authResult)) return authResult
  const { session } = authResult

  // 2. 获取 id 参数
  const { id } = await ctx.params
  if (!id) {
    throw new ApiError('INVALID_PARAMS', { message: '风格 ID 不能为空' })
  }

  // 3. 验证风格存在且属于当前用户
  const existing = await prisma.userStyle.findUnique({
    where: { id },
    select: { id: true, userId: true, isSystem: true },
  })
  if (!existing) {
    throw new ApiError('NOT_FOUND', { message: '风格不存在' })
  }
  if (existing.userId !== session.user.id) {
    throw new ApiError('FORBIDDEN', { message: '无权访问此风格' })
  }
  if (existing.isSystem) {
    throw new ApiError('FORBIDDEN', { message: '系统预设不可修改' })
  }

  // 4. 解析 multipart/form-data
  const formData = await request.formData()
  const file = formData.get('file') as File | null
  if (!file) {
    throw new ApiError('INVALID_PARAMS', { message: '请上传图片文件' })
  }

  // 5. 验证文件类型
  const allowedTypes = ['image/jpeg', 'image/png', 'image/webp']
  if (!allowedTypes.includes(file.type)) {
    throw new ApiError('INVALID_PARAMS', {
      message: '仅支持 JPEG、PNG、WebP 格式图片',
    })
  }

  // 6. 验证文件大小 (最大 10MB)
  const maxSize = 10 * 1024 * 1024
  if (file.size > maxSize) {
    throw new ApiError('INVALID_PARAMS', {
      message: '图片大小不能超过 10MB',
    })
  }

  // 7. 上传到 S3
  const buffer = Buffer.from(await file.arrayBuffer())
  const key = `user-styles/${session.user.id}/${id}/ref.jpg`
  await uploadObject(buffer, key, 3, file.type)

  // 8. 更新风格记录，触发异步提取
  const updated = await prisma.userStyle.update({
    where: { id },
    data: {
      referenceImageUrl: key,
      extractionStatus: 'pending',
      extractionMessage: null,
    },
    select: {
      id: true,
      name: true,
      referenceImageUrl: true,
      extractionStatus: true,
    },
  })

  // 9. 触发异步提取任务（Task 3 会添加队列）
  // TODO: 添加队列触发

  return NextResponse.json({ style: updated })
})
```
  </action>
  <verify>
    <automated>cd "/Users/gaoziwen/vibe coding/导演终结者" && npx tsc --noEmit src/app/api/user-styles/[id]/upload-ref/route.ts 2>&1 | head -20</automated>
  </verify>
  <done>
    src/app/api/user-styles/[id]/upload-ref/route.ts 存在
    包含 POST 处理器
    导入 uploadObject from @/lib/storage
    TypeScript 编译无错误
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建异步提取 Worker — style-extract-task-handler.ts</name>
  <files>src/lib/workers/handlers/style-extract-task-handler.ts, src/lib/workers/index.ts</files>
  <read_first>
    - src/lib/llm/vision.ts (chatCompletionWithVision)
    - src/lib/workers/handlers/panel-image-task-handler.ts (Worker 模式参考)
    - src/lib/workers/index.ts (Worker 注册)
    - src/lib/styles/style-service.ts (updateUserStyle)
  </read_first>
  <action>
创建 `src/lib/workers/handlers/style-extract-task-handler.ts`：

```typescript
/**
 * 风格提取 Worker Handler
 * 从参考图中提取风格描述
 */

import { Job } from 'bullmq'
import { prisma } from '@/lib/prisma'
import { chatCompletionWithVision } from '@/lib/llm/vision'
import { toFetchableUrl } from '@/lib/storage'
import { logInfo, logError } from '@/lib/logging/core'

const STYLE_EXTRACT_PROMPT_ZH = `请分析这张图片的视觉风格特征，描述以下方面（不要描述图片内容）：

1. 线条风格：粗细、硬度、连续性
2. 上色方式：平涂、渐变、赛璐璐、厚涂等
3. 光影处理：对比度、光源方向、阴影风格
4. 构图特点：视角、透视、空间感
5. 整体氛围：写实、卡通、动漫、油画等

请用中文输出一段 50-100 字的风格描述。`

const STYLE_EXTRACT_PROMPT_EN = `Analyze the visual style characteristics of this image. Describe (do NOT describe the image content):

1. Line style: thickness, hardness, continuity
2. Coloring method: flat, gradient, cel-shading, impasto, etc.
3. Lighting: contrast, light direction, shadow style
4. Composition: angle, perspective, spatial sense
5. Overall atmosphere: realistic, cartoon, anime, oil painting, etc.

Output a 50-100 word style description in English.`

interface StyleExtractJobData {
  styleId: string
  userId: string
  referenceImageUrl: string
  analysisModel: string | null
}

export async function styleExtractTaskHandler(job: Job<StyleExtractJobData>) {
  const { styleId, userId, referenceImageUrl, analysisModel } = job.data

  logInfo('[style-extract]', `开始提取风格 ${styleId}`, {
    userId,
    referenceImageUrl,
  })

  try {
    // 1. 获取可访问的图片 URL
    const imageUrl = toFetchableUrl(referenceImageUrl)

    // 2. 调用 LLM Vision 提取中文描述
    const zhCompletion = await chatCompletionWithVision(
      userId,
      analysisModel,
      STYLE_EXTRACT_PROMPT_ZH,
      [imageUrl],
    )
    const promptZh = zhCompletion.choices[0]?.message?.content?.trim() || ''

    // 3. 调用 LLM Vision 提取英文描述
    const enCompletion = await chatCompletionWithVision(
      userId,
      analysisModel,
      STYLE_EXTRACT_PROMPT_EN,
      [imageUrl],
    )
    const promptEn = enCompletion.choices[0]?.message?.content?.trim() || ''

    // 4. 更新风格记录
    await prisma.userStyle.update({
      where: { id: styleId },
      data: {
        promptZh,
        promptEn,
        extractionStatus: 'completed',
        extractionMessage: null,
      },
    })

    logInfo('[style-extract]', `风格提取完成 ${styleId}`, {
      promptZhLength: promptZh.length,
      promptEnLength: promptEn.length,
    })

    return { success: true, promptZh, promptEn }
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : '提取失败'

    // 更新状态为失败
    await prisma.userStyle.update({
      where: { id: styleId },
      data: {
        extractionStatus: 'failed',
        extractionMessage: errorMessage,
      },
    })

    logError('[style-extract]', `风格提取失败 ${styleId}`, { error: errorMessage })
    throw error
  }
}
```

然后在 `src/lib/workers/index.ts` 中注册队列和 worker：

```typescript
// 添加到 imports
import { styleExtractTaskHandler } from './handlers/style-extract-task-handler'

// 添加队列创建
export const styleExtractQueue = new Queue('style-extract', { connection })

// 添加 worker 注册
const styleExtractWorker = new Worker('style-extract', styleExtractTaskHandler, {
  connection,
  concurrency: 2,
})
```
  </action>
  <verify>
    <automated>cd "/Users/gaoziwen/vibe coding/导演终结者" && npx tsc --noEmit src/lib/workers/handlers/style-extract-task-handler.ts 2>&1 | head -20</automated>
  </verify>
  <done>
    style-extract-task-handler.ts 存在并导出 styleExtractTaskHandler
    workers/index.ts 包含 styleExtractQueue
    TypeScript 编译无错误
  </done>
</task>

<task type="auto">
  <name>Task 4: 在上传 API 中触发异步提取任务</name>
  <files>src/app/api/user-styles/[id]/upload-ref/route.ts</files>
  <read_first>
    - src/app/api/user-styles/[id]/upload-ref/route.ts (Task 2 创建)
    - src/lib/workers/index.ts (styleExtractQueue)
  </read_first>
  <action>
修改 `src/app/api/user-styles/[id]/upload-ref/route.ts`，在 Step 9 添加队列触发：

```typescript
// 在文件顶部添加 import
import { styleExtractQueue } from '@/lib/workers'
import { getUserPreference } from '@/lib/user-preference/service'

// 在 Step 9 替换 TODO 为实际代码
// 9. 触发异步提取任务
const userPref = await getUserPreference(session.user.id)
await styleExtractQueue.add('extract', {
  styleId: id,
  userId: session.user.id,
  referenceImageUrl: key,
  analysisModel: userPref.analysisModel,
})
```
  </action>
  <verify>
    <automated>cd "/Users/gaoziwen/vibe coding/导演终结者" && grep -n "styleExtractQueue.add" src/app/api/user-styles/[id]/upload-ref/route.ts</automated>
  </verify>
  <done>
    upload-ref/route.ts 包含 styleExtractQueue.add 调用
    TypeScript 编译无错误
  </done>
</task>

<task type="auto">
  <name>Task 5: 添加单元测试</name>
  <files>tests/unit/style-extract.test.ts</files>
  <read_first>
    - tests/unit/style-service.test.ts (现有测试模式)
    - src/lib/workers/handlers/style-extract-task-handler.ts
  </read_first>
  <action>
创建 `tests/unit/style-extract.test.ts`：

```typescript
import { describe, expect, it, vi, beforeEach } from 'vitest'

// Mock prisma
vi.mock('@/lib/prisma', () => ({
  prisma: {
    userStyle: {
      update: vi.fn(),
    },
  },
}))

// Mock chatCompletionWithVision
vi.mock('@/lib/llm/vision', () => ({
  chatCompletionWithVision: vi.fn().mockResolvedValue({
    choices: [{ message: { content: 'mocked style description' } }],
  }),
}))

// Mock storage
vi.mock('@/lib/storage', () => ({
  toFetchableUrl: (url: string) => url,
}))

import { prisma } from '@/lib/prisma'
import { chatCompletionWithVision } from '@/lib/llm/vision'
import { styleExtractTaskHandler } from '@/lib/workers/handlers/style-extract-task-handler'

describe('styleExtractTaskHandler', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  it('成功提取风格描述并更新 DB', async () => {
    vi.mocked(prisma.userStyle.update).mockResolvedValue({} as never)

    const job = {
      data: {
        styleId: 'style-123',
        userId: 'user-456',
        referenceImageUrl: 'user-styles/user-456/style-123/ref.jpg',
        analysisModel: 'gpt-4o',
      },
    } as any

    const result = await styleExtractTaskHandler(job)

    expect(result.success).toBe(true)
    expect(prisma.userStyle.update).toHaveBeenCalledWith(
      expect.objectContaining({
        where: { id: 'style-123' },
        data: expect.objectContaining({
          extractionStatus: 'completed',
        }),
      }),
    )
    expect(chatCompletionWithVision).toHaveBeenCalledTimes(2)
  })

  it('提取失败时更新 extractionStatus 为 failed', async () => {
    vi.mocked(chatCompletionWithVision).mockRejectedValue(new Error('API Error'))
    vi.mocked(prisma.userStyle.update).mockResolvedValue({} as never)

    const job = {
      data: {
        styleId: 'style-123',
        userId: 'user-456',
        referenceImageUrl: 'user-styles/user-456/style-123/ref.jpg',
        analysisModel: 'gpt-4o',
      },
    } as any

    await expect(styleExtractTaskHandler(job)).rejects.toThrow('API Error')

    expect(prisma.userStyle.update).toHaveBeenCalledWith(
      expect.objectContaining({
        where: { id: 'style-123' },
        data: expect.objectContaining({
          extractionStatus: 'failed',
          extractionMessage: 'API Error',
        }),
      }),
    )
  })
})
```
  </action>
  <verify>
    <automated>cd "/Users/gaoziwen/vibe coding/导演终结者" && npm test -- --filter=style-extract --run 2>&1 | tail -30</automated>
  </verify>
  <done>
    tests/unit/style-extract.test.ts 存在
    npm test 通过所有 style-extract 测试
  </done>
</task>

</tasks>

<verification>
1. **Schema 迁移**：extractionStatus 和 extractionMessage 字段存在
2. **上传 API**：POST /api/user-styles/:id/upload-ref 返回 200
3. **Worker**：styleExtractQueue 和 styleExtractWorker 注册
4. **异步触发**：上传后自动触发队列任务
5. **状态追踪**：pending → completed/failed 状态正确更新
6. **单元测试**：npm test 通过
</verification>

<success_criteria>
1. 用户可以上传图片文件到 S3
2. LLM Vision 提取风格特征而非内容
3. 三态追踪：pending/completed/failed
4. 用户可编辑 AI 生成的草稿
5. 提取失败显示错误信息
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai/07-01-SUMMARY.md`
</output>
