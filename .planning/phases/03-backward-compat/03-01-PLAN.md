---
phase: "03"
plan: "01"
name: 03-backward-compat-01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/integration/backward-compat.test.ts
  - src/lib/styles/style-service.ts
  - src/types/project.ts
  - prisma/schema.prisma
autonomous: false
requirements:
  - INTEG-01
  - INTEG-02
must_haves:
  truths:
    - "现有项目的 artStyle 值经 resolveStylePrompt 解析后与 getArtStylePrompt 结果一致"
    - "系统预设记录不可被修改或删除，服务端返回 403"
    - "artStylePrompt 字段所有读取路径已标记废弃"
  artifacts:
    - path: "tests/integration/backward-compat.test.ts"
      provides: "向后兼容集成测试，8 个断言验证 4 预设 x 2 locale"
    - path: "src/lib/styles/style-service.ts"
      provides: "assertUserStyleNotSystem 保护函数"
      exports: ["assertUserStyleNotSystem"]
    - path: "src/types/project.ts"
      provides: "artStylePrompt 废弃注释"
    - path: "prisma/schema.prisma"
      provides: "artStylePrompt 废弃注释"
  key_links:
    - from: "src/lib/styles/style-service.ts"
      to: "prisma.userStyle"
      via: "prisma.userStyle.findUnique"
      pattern: "prisma\\.userStyle\\.findUnique"
    - from: "tests/integration/backward-compat.test.ts"
      to: "src/lib/styles/style-resolver.ts"
      via: "resolveStylePrompt import"
      pattern: "resolveStylePrompt"
---

<objective>
建立向后兼容验证和系统预设保护机制。

Purpose: 验证现有项目 artStyle 数据（"american-comic" 等）经新解析器解析后与原函数结果一致，同时在服务层建立系统预设不可修改/删除的保护函数。

Output: 一个 TDD 计划（RED 测试 + GREEN 实现）+ 一个审计清理任务
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/styles/style-resolver.ts
@src/lib/constants.ts
@src/lib/styles/style-namespace.ts
@src/lib/api-errors.ts
@src/lib/errors/codes.ts
@src/types/project.ts
@prisma/schema.prisma
@tests/integration/user-style.test.ts
@tests/unit/style-resolver.test.ts
</context>

<intent>
Phase 3 是验证 + 保护两个正交关注点。向后兼容验证是 INTEG-02，系统预设保护是 INTEG-01。两者没有依赖关系，但按 plan 只做 1 个。

采用 TDD plan type: Task 0 = RED（写测试，测试失败），Task 1 = GREEN（实现功能，测试通过），Task 2 = 审计与废弃标记（无 TDD）。
</intent>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (TDD RED): 写向后兼容集成测试</name>
  <files>tests/integration/backward-compat.test.ts</files>
  <read_first>
    tests/integration/user-style.test.ts
    src/lib/styles/style-resolver.ts
    src/lib/constants.ts
  </read_first>
  <behavior>
    - Test 1: resolveStylePrompt('american-comic', userId, 'zh') === getArtStylePrompt('american-comic', 'zh')
    - Test 2: resolveStylePrompt('american-comic', userId, 'en') === getArtStylePrompt('american-comic', 'en')
    - Test 3: resolveStylePrompt('chinese-comic', userId, 'zh') === getArtStylePrompt('chinese-comic', 'zh')
    - Test 4: resolveStylePrompt('chinese-comic', userId, 'en') === getArtStylePrompt('chinese-comic', 'en')
    - Test 5: resolveStylePrompt('japanese-anime', userId, 'zh') === getArtStylePrompt('japanese-anime', 'zh')
    - Test 6: resolveStylePrompt('japanese-anime', userId, 'en') === getArtStylePrompt('japanese-anime', 'en')
    - Test 7: resolveStylePrompt('realistic', userId, 'zh') === getArtStylePrompt('realistic', 'zh')
    - Test 8: resolveStylePrompt('realistic', userId, 'en') === getArtStylePrompt('realistic', 'en')
    注意: resolveStylePrompt 返回 Promise&lt;string | null&gt;，getArtStylePrompt 返回 string，需 await 后比较
  </behavior>
  <action>
    创建 tests/integration/backward-compat.test.ts。

    格式要求（遵循 tests/integration/user-style.test.ts 模式）：
    1. 从 tests/helpers/prisma 导入 prisma
    2. 使用 beforeEach 清理数据（删 userStyles，再删 users）
    3. 使用 randomUUID() 避免用户名冲突
    4. 不需要创建真实数据库记录（预设风格路径不查库，只需 userId 参数合法）
    5. 每个断言使用 expect(await resolveStylePrompt(...)).toBe(getArtStylePrompt(...))

    导入:
    - resolveStylePrompt from '@/lib/styles/style-resolver'
    - getArtStylePrompt from '@/lib/constants'
    - prisma from '@/tests/helpers/prisma'
    - randomUUID from 'node:crypto'

    注意: 不需要 vi.mock，因为预设风格路径不查 Prisma。但需要创建用户以便传入有效 userId。
  </action>
  <verify>
    <automated>npm test -- --filter="backward-compat" 2>&1 | head -30</automated>
  </verify>
  <done>
    tests/integration/backward-compat.test.ts 存在，包含 8 个测试用例。运行 npm test -- --filter=backward-compat 时所有测试 FAILED（RED phase）。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (TDD GREEN): 实现 assertUserStyleNotSystem 保护函数</name>
  <files>src/lib/styles/style-service.ts</files>
  <read_first>
    src/lib/api-errors.ts
    src/lib/errors/codes.ts
    src/lib/prisma.ts
  </read_first>
  <behavior>
    - Test 1: assertUserStyleNotSystem(userStyleId, userId) 当记录 isSystem=false 时 resolve（无抛出）
    - Test 2: assertUserStyleNotSystem(userStyleId, userId) 当记录 isSystem=true 时抛出 ApiError，status=403
    - Test 3: assertUserStyleNotSystem(userStyleId, userId) 当记录不存在时 resolve（记录不存在时不做 isSystem 检查）
    - Test 4: 任意 userId 调用均正确（userId 不影响 isSystem 检查逻辑）
  </behavior>
  <action>
    创建 src/lib/styles/style-service.ts。

    函数签名（per D-18）:
    ```typescript
    import { prisma } from '@/lib/prisma'
    import { ApiError } from '@/lib/api-errors'

    /**
     * 断言 UserStyle 记录不是系统预设。
     * 当 isSystem=true 时抛出 403 错误，防止删除/编辑系统预设。
     *
     * @param userStyleId - UserStyle 记录的 id
     * @param userId - 当前用户 ID（用于查询）
     * @throws ApiError('STYLE_SYSTEM_NOT_MODIFIABLE') 当记录是系统预设时
     */
    export async function assertUserStyleNotSystem(
      userStyleId: string,
      userId: string,
    ): Promise<void> {
      const style = await prisma.userStyle.findUnique({
        where: { id: userStyleId, userId },
        select: { isSystem: true },
      })

      if (style?.isSystem === true) {
        throw new ApiError('STYLE_SYSTEM_NOT_MODIFIABLE')
      }
    }
    ```

    重要说明:
    - 仅当 isSystem === true 时才抛出（per D-16），null/undefined/false 均不抛出
    - 记录不存在时不抛出（留给 Phase 5 API 层处理 404）
    - 错误码: 'STYLE_SYSTEM_NOT_MODIFIABLE'（D-19）
    - ApiError 构造函数在 code 不在 ERROR_CATALOG 中时会降级到 INTERNAL_ERROR (status=500)，不符合预期
    - 正确做法: 使用 ApiError('FORBIDDEN', { message: '系统预设不可修改或删除', styleId: userStyleId })
      - ERROR_CATALOG.FORBIDDEN.httpStatus === 403
      - details.message 会覆盖默认消息
      - details.styleId 供 Phase 5 日志记录使用
    - 追加导出常量: export const STYLE_SYSTEM_NOT_MODIFIABLE_CODE = 'STYLE_SYSTEM_NOT_MODIFIABLE' as const
  </action>
  <verify>
    <automated>npm test -- --filter="style-service" 2>&1 | head -30</automated>
  </verify>
  <done>
    src/lib/styles/style-service.ts 存在，包含 assertUserStyleNotSystem 函数。npm test -- --filter=style-service 所有测试 PASSED。
  </done>
</task>

<task type="auto">
  <name>Task 2: 审计 artStylePrompt 读取路径并标记废弃</name>
  <files>src/types/project.ts, prisma/schema.prisma</files>
  <read_first>
    src/types/project.ts
    prisma/schema.prisma
    src/lib/workers/handlers/reference-to-character.ts
  </read_first>
  <acceptance_criteria>
    - grep "🔥 废弃" src/types/project.ts 包含 artStylePrompt 的废弃注释
    - grep "🔥 废弃" prisma/schema.prisma 包含 artStylePrompt 的废弃注释
    - grep "🔥 废弃" src/lib/workers/handlers/reference-to-character.ts 包含废弃注释
  </acceptance_criteria>
  <action>
    Step A: 全局 grep "artStylePrompt" 在 src/ 目录，逐一确认读取路径状态。

    Step B: 对每个读取路径代码，添加 "🔥 废弃: Phase 3 确认不再读取，详见 D-11/D-20" 注释。

    当前已知读取路径:
    1. src/types/project.ts:263 - 类型定义字段，非运行时读取，标记废弃
    2. src/app/api/projects/route.ts:205 - 已是注释，无需操作
    3. src/lib/workers/handlers/reference-to-character.ts:194 - 真实读取路径，添加废弃注释

    Step C: 在 src/types/project.ts 的 artStylePrompt 字段定义处添加废弃注释:
    ```typescript
    // 🔥 废弃 (Phase 3): 此字段不再写入也不再读取。
    // 实时风格 prompt 统一通过 resolveStylePrompt() 获取（Phase 2）。
    // 将在后续迁移中从 schema 删除。
    artStylePrompt: string | null
    ```

    Step D: 在 prisma/schema.prisma 的 NovelPromotionProject 模型的 artStylePrompt 字段添加废弃注释:
    ```prisma
    // 🔥 废弃 (Phase 3): 实时风格 prompt 统一通过 resolveStylePrompt() 获取。
    // 此字段将在后续迁移中删除。
    artStylePrompt  String?  @db.Text
    ```

    Step E: 在 reference-to-character.ts 第 194 行 artStylePrompt 读取代码上方添加废弃注释。
    注意: 不要修改任何功能代码，只加注释。
  </action>
  <verify>
    <automated>grep -n "🔥 废弃" src/types/project.ts prisma/schema.prisma src/lib/workers/handlers/reference-to-character.ts</automated>
  </verify>
  <done>
    src/types/project.ts artStylePrompt 字段有废弃注释，prisma/schema.prisma artStylePrompt 字段有废弃注释，reference-to-character.ts 的读取路径有废弃注释。
  </done>
</task>

</tasks>

<verification>
整体验证步骤:

1. npm test -- --filter=backward-compat → 所有 8 个测试 PASSED（INTEG-02）
2. npm test -- --filter=style-service → 所有测试 PASSED（INTEG-01）
3. grep "🔥 废弃" src/types/project.ts → 包含 artStylePrompt 的废弃注释
4. grep "🔥 废弃" prisma/schema.prisma → 包含 artStylePrompt 的废弃注释
5. grep "🔥 废弃" src/lib/workers/handlers/reference-to-character.ts → 包含 artStylePrompt 读取路径的废弃注释
6. npm run type-check（或 tsc --noEmit）→ 无类型错误
</verification>

<success_criteria>
Phase 3 完成标准（per ROADMAP.md）:

1. INTEG-02: 数据库中所有现有项目的 artStyle 字段值（"american-comic" 等）经新解析器解析后与原来结果一致
   → 验证: tests/integration/backward-compat.test.ts 8 个测试全部通过

2. INTEG-01: 4 个系统预设风格在 StyleSelector 中始终可见且不可删除（UI 层禁用删除按钮）
   → Phase 3 不涉及 UI 层，此标准在 Phase 8 的 StyleSelector 扩展中实现

3. INTEG-01 (服务端): DELETE /api/user-styles/:id 对系统预设 ID 调用时返回 403，服务端拒绝操作
   → 验证: assertUserStyleNotSystem() 抛出 403 错误，Phase 5 API 层会调用此函数

4. INTEG-01 (服务端): PUT /api/user-styles/:id 对系统预设 ID 调用时返回 403，服务端拒绝编辑
   → 验证: 同上 assertUserStyleNotSystem()，Phase 5 API 层会调用此函数

5. artStylePrompt 无任何读取路径，且类型定义和 schema 均已标记废弃
   → 验证: grep 结果确认所有读取路径有废弃注释
</success_criteria>

<output>
After completion, create `.planning/phases/03-backward-compat/03-01-SUMMARY.md`
</output>
