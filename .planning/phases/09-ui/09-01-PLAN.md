---
phase: 09-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/profile/page.tsx
  - src/app/[locale]/profile/components/StylesTab.tsx
  - src/components/styles/StyleManager.tsx
  - src/components/styles/StyleCard.tsx
  - src/hooks/useUserStyles.ts
  - messages/zh/profile.json
  - messages/en/profile.json
autonomous: true
requirements:
  - STYLE-05
  - INTEG-03
user_setup: []
must_haves:
  truths:
    - "用户可以打开风格管理页面，看到自己所有自定义风格的列表"
    - "用户可以删除自定义风格，删除后风格列表立即更新"
    - "用户风格数量达到上限时，新建风格按钮禁用并显示上限提示"
  artifacts:
    - path: "src/app/[locale]/profile/page.tsx"
      provides: "Profile 页面添加风格管理 Tab"
      contains: "activeSection === 'styles'"
    - path: "src/components/styles/StyleManager.tsx"
      provides: "风格列表管理组件"
      exports: ["StyleManager"]
      min_lines: 80
    - path: "src/components/styles/StyleCard.tsx"
      provides: "单个风格卡片展示"
      exports: ["StyleCard"]
      min_lines: 40
  key_links:
    - from: "src/app/[locale]/profile/page.tsx"
      to: "src/components/styles/StyleManager.tsx"
      via: "import StyleManager"
      pattern: "StyleManager"
    - from: "src/components/styles/StyleManager.tsx"
      to: "/api/user-styles"
      via: "useUserStyles hook"
      pattern: "useUserStyles"
---

<objective>
在 Profile 页面添加"风格管理"Tab，实现风格列表展示和删除功能。

Purpose: 为用户提供自定义风格的可视化管理入口，展示所有风格卡片并支持删除操作。
Output: StyleManager 组件、StyleCard 组件、StylesTab 集成到 Profile 页面。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 现有组件模式参考

## Profile 页面结构 (src/app/[locale]/profile/page.tsx)
- 使用 useState 管理活动分区: `activeSection: 'billing' | 'apiConfig'`
- 左侧侧边栏导航按钮
- 右侧内容区根据 activeSection 渲染对应组件

## GlassModalShell (src/components/ui/primitives/GlassModalShell.tsx)
- 弹窗基础壳，支持 size、title、footer 等属性
- 通过 createPortal 渲染到 document.body

## useUserStyles Hook (src/hooks/useUserStyles.ts)
- 返回 `{ styles, options, loading }`
- 未登录时返回空数组，不发起请求
- 已登录时调用 GET /api/user-styles 获取风格列表

## ConfigDeleteModal (src/components/ui/config-modals/ConfigDeleteModal.tsx)
- 封装 ConfigConfirmModal，支持 danger 样式
- 用于删除确认操作

## style-service.ts 常量
- `MAX_STYLE_LIMIT = 20` - 每用户最大风格数量

## Prisma UserStyle 模型
```prisma
model UserStyle {
  id                String   @id @default(uuid())
  userId            String
  name              String
  promptZh          String   @db.Text
  promptEn          String   @db.Text
  tags              String?  @db.Text
  referenceImageUrl String?  @db.Text
  isSystem          Boolean  @default(false)
  extractionStatus  String   @default("pending")
  extractionMessage String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @default(now()) @updatedAt
}
```

## API 端点
- GET /api/user-styles - 获取当前用户所有风格
- DELETE /api/user-styles/:id - 删除指定风格
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/hooks/useUserStyles.ts:
```typescript
export interface UserStyle {
  id: string
  name: string
  promptZh: string
  promptEn: string
  tags: string | null
  referenceImageUrl: string | null
  createdAt: Date
  updatedAt: Date
}

export interface UseUserStylesReturn {
  styles: UserStyle[]
  options: StyleOption[]
  loading: boolean
}
```

From src/lib/storage/index.ts:
```typescript
export function toFetchableUrl(inputUrl: string): string
```

From src/contexts/ToastContext.tsx:
```typescript
export interface ToastContextValue {
  showToast: (message: string, type?: Toast['type'], duration?: number) => void
  showError: (code: string, details?: Record<string, unknown>) => void
  dismissToast: (id: string) => void
}

export function useToast(): ToastContextValue
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 扩展 Profile 页面添加风格管理 Tab</name>
  <files>
    src/app/[locale]/profile/page.tsx
    messages/zh/profile.json
    messages/en/profile.json
  </files>
  <read_first>
    src/app/[locale]/profile/page.tsx
    messages/zh/profile.json
    messages/en/profile.json
  </read_first>
  <action>
    1. 在 Profile 页面的 activeSection 类型中添加 'styles' 选项：
       - 修改 `useState<'billing' | 'apiConfig'>` 为 `useState<'billing' | 'apiConfig' | 'styles'>`

    2. 在左侧导航菜单中添加"风格管理"按钮（在 API 配置和扣费记录之间）：
       - 使用 AppIcon name="palette" 作为图标
       - 使用 t('styles') 获取国际化文案

    3. 在右侧内容区添加 StylesTab 渲染条件：
       - `{activeSection === 'styles' && <StylesTab />}`

    4. 在 messages/zh/profile.json 中添加文案：
       ```json
       "styles": "风格管理",
       "styleCount": "当前 {count}/{limit} 个风格",
       "styleLimitReached": "已达到风格数量上限（{limit} 个）"
       ```

    5. 在 messages/en/profile.json 中添加对应英文文案（如文件存在）。

    参考 profile/page.tsx 中现有的导航按钮样式和结构。
  </action>
  <verify>
    <automated>grep -n "activeSection === 'styles'" src/app/[locale]/profile/page.tsx</automated>
  </verify>
  <done>
    - activeSection 类型包含 'styles'
    - 左侧导航栏有"风格管理"按钮
    - 右侧内容区有 StylesTab 渲染条件
    - 国际化文案已添加
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 StyleCard 组件</name>
  <files>
    src/components/styles/StyleCard.tsx
  </files>
  <read_first>
    src/hooks/useUserStyles.ts
    src/lib/storage/index.ts
  </read_first>
  <action>
    创建 src/components/styles/StyleCard.tsx 组件，用于展示单个风格卡片。

    Props 接口：
    ```typescript
    interface StyleCardProps {
      style: UserStyle
      onEdit: (id: string) => void
      onDelete: (id: string) => void
    }
    ```

    组件结构：
    1. 外层容器使用 `glass-surface-soft rounded-xl border border-[var(--glass-stroke-base)] p-4`
    2. 左侧展示参考图缩略图（如有），使用 toFetchableUrl 转换 URL
       - 尺寸: w-16 h-16 rounded-lg object-cover
       - 无参考图时显示占位图标（AppIcon name="image"）
    3. 右侧信息区：
       - 风格名称（font-medium text-[var(--glass-text-primary)]）
       - 标签展示（如有），使用 chip 样式，最多显示 3 个标签
       - 创建时间（text-xs text-[var(--glass-text-tertiary)]）
    4. 操作按钮区：
       - 编辑按钮（AppIcon name="pencil"）
       - 删除按钮（AppIcon name="trash"）
       - 使用 glass-btn-base glass-btn-ghost 样式

    文件头部添加：
    ```typescript
    'use client'
    /**
     * 风格卡片组件
     * 展示单个自定义风格的信息和操作按钮
     */
    ```
  </action>
  <verify>
    <automated>grep -n "StyleCardProps" src/components/styles/StyleCard.tsx</automated>
  </verify>
  <done>
    - StyleCard 组件已创建
    - 支持展示参考图缩略图
    - 支持展示标签
    - 包含编辑和删除按钮
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 创建 StyleManager 组件</name>
  <files>
    src/components/styles/StyleManager.tsx
  </files>
  <read_first>
    src/hooks/useUserStyles.ts
    src/components/styles/StyleCard.tsx
    src/components/ui/config-modals/ConfigDeleteModal.tsx
    src/contexts/ToastContext.tsx
    src/lib/styles/style-service.ts
    src/lib/api-fetch.ts
  </read_first>
  <action>
    创建 src/components/styles/StyleManager.tsx 组件，实现风格列表管理和删除功能。

    功能要求：
    1. 使用 useUserStyles hook 获取风格列表
    2. 使用 useToast 处理操作反馈
    3. 使用 useTranslations('profile') 获取国际化文案
    4. 实现删除确认弹窗（使用 ConfigDeleteModal）
    5. 实现风格数量上限检查

    组件结构：
    ```tsx
    'use client'

    import { useState } from 'react'
    import { useTranslations } from 'next-intl'
    import { useSession } from 'next-auth/react'
    import { AppIcon } from '@/components/ui/icons'
    import { useUserStyles, type UserStyle } from '@/hooks/useUserStyles'
    import { useToast } from '@/contexts/ToastContext'
    import { apiFetch } from '@/lib/api-fetch'
    import { StyleCard } from './StyleCard'
    import { ConfigDeleteModal } from '@/components/ui/config-modals/ConfigDeleteModal'
    import { MAX_STYLE_LIMIT } from '@/lib/styles/style-service'

    export function StyleManager() {
      const { status } = useSession()
      const { styles, loading } = useUserStyles()
      const { showToast, showError } = useToast()
      const t = useTranslations('profile')

      const [deleteTarget, setDeleteTarget] = useState<UserStyle | null>(null)
      const [isDeleting, setIsDeleting] = useState(false)

      // 未登录提示
      if (status !== 'authenticated') {
        return <LoginPrompt />
      }

      // 加载状态
      if (loading) {
        return <LoadingState />
      }

      // 删除处理
      const handleDelete = async () => {
        if (!deleteTarget) return
        setIsDeleting(true)
        try {
          const res = await apiFetch(`/api/user-styles/${deleteTarget.id}`, {
            method: 'DELETE',
          })
          if (!res.ok) {
            const error = await res.json()
            showError(error.code || 'DELETE_FAILED')
            return
          }
          showToast(t('styleDeleted'), 'success')
          // 刷新列表（通过重新调用 useUserStyles 的内部刷新）
        } catch (error) {
          showError('NETWORK_ERROR')
        } finally {
          setIsDeleting(false)
          setDeleteTarget(null)
        }
      }

      // 计算是否达到上限
      const isLimitReached = styles.length >= MAX_STYLE_LIMIT

      return (
        <div className="flex flex-col h-full">
          {/* 顶部工具栏 */}
          <div className="flex items-center justify-between mb-4">
            <div>
              <h2 className="text-lg font-semibold">{t('styles')}</h2>
              <p className="text-sm text-[var(--glass-text-secondary)]">
                {t('styleCount', { count: styles.length, limit: MAX_STYLE_LIMIT })}
              </p>
            </div>
            <button
              onClick={() => {/* 打开创建弹窗，Phase 9 Plan 02 实现 */}}
              disabled={isLimitReached}
              className="glass-btn-base glass-btn-primary px-4 py-2"
            >
              <AppIcon name="plus" className="w-4 h-4 mr-2" />
              {t('createStyle')}
            </button>
          </div>

          {/* 上限提示 */}
          {isLimitReached && (
            <div className="mb-4 px-3 py-2 rounded-lg bg-[var(--glass-tone-warning-bg)] text-[var(--glass-tone-warning-fg)] text-sm">
              {t('styleLimitReached', { limit: MAX_STYLE_LIMIT })}
            </div>
          )}

          {/* 风格列表 */}
          <div className="flex-1 overflow-auto">
            {styles.length === 0 ? (
              <EmptyState />
            ) : (
              <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
                {styles.map(style => (
                  <StyleCard
                    key={style.id}
                    style={style}
                    onEdit={(id) => {/* Phase 9 Plan 02 实现 */}}
                    onDelete={(id) => setDeleteTarget(styles.find(s => s.id === id) || null)}
                  />
                ))}
              </div>
            )}
          </div>

          {/* 删除确认弹窗 */}
          <ConfigDeleteModal
            isOpen={!!deleteTarget}
            onClose={() => setDeleteTarget(null)}
            onDelete={handleDelete}
            title={t('deleteStyleTitle')}
            description={t('deleteStyleDesc', { name: deleteTarget?.name })}
            deleteDisabled={isDeleting}
          />
        </div>
      )
    }
    ```

    注意：
    - LoginPrompt、LoadingState、EmptyState 可以内联实现或提取为子组件
    - createStyle、styleDeleted、deleteStyleTitle、deleteStyleDesc 文案需添加到 profile.json
    - 新建按钮的点击处理暂时留空，Plan 02 实现
  </action>
  <verify>
    <automated>grep -n "StyleManager" src/components/styles/StyleManager.tsx && grep -n "ConfigDeleteModal" src/components/styles/StyleManager.tsx</automated>
  </verify>
  <done>
    - StyleManager 组件已创建
    - 使用 useUserStyles 获取风格列表
    - 实现删除确认和删除操作
    - 实现风格数量上限检查和提示
    - 新建按钮在上限时禁用
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: 创建 StylesTab 组件并集成</name>
  <files>
    src/app/[locale]/profile/components/StylesTab.tsx
    messages/zh/profile.json
  </files>
  <read_first>
    src/components/styles/StyleManager.tsx
    src/app/[locale]/profile/components/ApiConfigTab.tsx
    messages/zh/profile.json
  </read_first>
  <action>
    1. 创建 src/app/[locale]/profile/components/StylesTab.tsx：
       ```tsx
       'use client'

       import { StyleManager } from '@/components/styles/StyleManager'

       export default function StylesTab() {
         return (
           <div className="h-full p-6">
             <StyleManager />
           </div>
         )
       }
       ```

    2. 在 profile/page.tsx 中导入 StylesTab：
       - 添加 `import StylesTab from './components/StylesTab'`
       - 确保 activeSection === 'styles' 时渲染 StylesTab

    3. 完善 messages/zh/profile.json 文案：
       ```json
       "createStyle": "新建风格",
       "editStyle": "编辑风格",
       "deleteStyle": "删除风格",
       "deleteStyleTitle": "确认删除风格",
       "deleteStyleDesc": "确定要删除风格「{name}」吗？此操作不可撤销。",
       "styleDeleted": "风格已删除",
       "noStyles": "暂无自定义风格",
       "noStylesDesc": "点击上方按钮创建您的第一个风格"
       ```
  </action>
  <verify>
    <automated>grep -n "StylesTab" src/app/[locale]/profile/page.tsx && grep -n "StyleManager" src/app/[locale]/profile/components/StylesTab.tsx</automated>
  </verify>
  <done>
    - StylesTab 组件已创建
    - Profile 页面正确导入和渲染 StylesTab
    - 国际化文案完整
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: 扩展 useUserStyles 支持刷新</name>
  <files>
    src/hooks/useUserStyles.ts
  </files>
  <read_first>
    src/hooks/useUserStyles.ts
  </read_first>
  <action>
    扩展 useUserStyles hook，添加 refresh 方法以支持删除后刷新列表。

    1. 修改 UseUserStylesReturn 接口：
       ```typescript
       export interface UseUserStylesReturn {
         styles: UserStyle[]
         options: StyleOption[]
         loading: boolean
         refresh: () => Promise<void>  // 新增
       }
       ```

    2. 将数据获取逻辑提取为独立的 fetchStyles 函数

    3. 在 useEffect 中调用 fetchStyles

    4. 返回 refresh 方法：
       ```typescript
       const refresh = useCallback(async () => {
         if (status !== 'authenticated') return
         setLoading(true)
         try {
           const res = await apiFetch('/api/user-styles')
           if (res.ok) {
             const data = await res.json()
             setStyles(data.styles ?? [])
           }
         } catch {
           // 静默处理
         } finally {
           setLoading(false)
         }
       }, [status])
       ```

    5. 更新 return 语句包含 refresh
  </action>
  <verify>
    <automated>grep -n "refresh" src/hooks/useUserStyles.ts</automated>
  </verify>
  <done>
    - useUserStyles 返回 refresh 方法
    - refresh 方法可重新获取风格列表
    - 删除操作后可调用 refresh 更新列表
  </done>
</task>

</tasks>

<verification>
1. Profile 页面左侧导航栏有"风格管理"按钮
2. 点击"风格管理"后右侧显示 StylesTab
3. StylesTab 展示当前用户的所有自定义风格
4. 每个风格卡片显示名称、标签、参考图缩略图
5. 点击删除按钮弹出确认弹窗
6. 确认删除后风格从列表中移除
7. 风格数量达到 20 时新建按钮禁用
8. 未登录时显示登录提示
</verification>

<success_criteria>
- 用户可以打开风格管理页面，看到自己所有自定义风格的列表（含标签和参考图缩略图）
- 用户可以删除自定义风格，删除后风格列表立即更新
- 用户风格数量达到上限时，"新建风格"按钮禁用并显示上限提示文案
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui/09-01-SUMMARY.md`
</output>
