---
phase: 09-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/components/styles/StyleCreateModal.tsx
  - src/components/styles/StyleTagSelector.tsx
  - src/components/styles/ReferenceImageUpload.tsx
  - src/components/styles/StyleManager.tsx
  - messages/zh/profile.json
autonomous: true
requirements:
  - STYLE-01
  - STYLE-02
  - STYLE-05
  - EXTRACT-01
  - EXTRACT-02
  - EXTRACT-03
  - EXTRACT-04
user_setup: []
must_haves:
  truths:
    - "用户可以点击新建风格，填写名称、提示词和标签后保存"
    - "用户可以在创建/编辑弹窗中上传参考图，等待 AI 提取草稿"
    - "用户可以编辑已有风格的名称、提示词和标签"
    - "AI 提取状态在前端有追踪：pending、completed、failed"
  artifacts:
    - path: "src/components/styles/StyleCreateModal.tsx"
      provides: "创建/编辑风格弹窗"
      exports: ["StyleCreateModal"]
      min_lines: 150
    - path: "src/components/styles/StyleTagSelector.tsx"
      provides: "标签选择器组件"
      exports: ["StyleTagSelector"]
      min_lines: 60
    - path: "src/components/styles/ReferenceImageUpload.tsx"
      provides: "参考图上传组件"
      exports: ["ReferenceImageUpload"]
      min_lines: 80
  key_links:
    - from: "src/components/styles/StyleCreateModal.tsx"
      to: "/api/user-styles"
      via: "apiFetch POST/PUT"
      pattern: "apiFetch.*user-styles"
    - from: "src/components/styles/ReferenceImageUpload.tsx"
      to: "/api/user-styles/:id/upload-ref"
      via: "apiFetch POST FormData"
      pattern: "upload-ref"
    - from: "StyleCreateModal"
      to: "StyleTagSelector"
      via: "import and render"
      pattern: "StyleTagSelector"
---

<objective>
实现风格创建/编辑弹窗，包含标签选择器和参考图上传功能，支持 AI 提取状态追踪。

Purpose: 让用户可以完整创建和编辑自定义风格，包括上传参考图并等待 AI 提取风格描述。
Output: StyleCreateModal 组件、StyleTagSelector 组件、ReferenceImageUpload 组件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 现有组件模式参考

## GlassModalShell (src/components/ui/primitives/GlassModalShell.tsx)
- 弹窗基础壳，支持 size、title、footer 等属性
- 通过 createPortal 渲染到 document.body

## API 端点
- POST /api/user-styles - 创建风格
- PUT /api/user-styles/:id - 更新风格
- POST /api/user-styles/:id/upload-ref - 上传参考图

## Zod Schema (src/lib/styles/style-schema.ts)
```typescript
export const createUserStyleSchema = z.object({
  name: z.string().min(1).max(50).trim(),
  promptZh: z.string().min(1).max(2000).trim(),
  promptEn: z.string().min(1).max(2000).trim(),
  tags: z.array(z.string()).optional(),
  referenceImageUrl: z.string().url().optional(),
})

export const updateUserStyleSchema = z.object({
  name: z.string().min(1).max(50).trim().optional(),
  promptZh: z.string().min(1).max(2000).trim().optional(),
  promptEn: z.string().min(1).max(2000).trim().optional(),
  tags: z.array(z.string()).optional(),
  referenceImageUrl: z.string().url().nullish().optional(),
})
```

## 预设标签列表
['写实', '动漫', '抽象', '油画', '水彩', '赛璐璐', '厚涂', '像素']

## AI 提取状态
- pending: 提取中
- completed: 提取完成，草稿就绪
- failed: 提取失败

## toFetchableUrl
用于将 S3 key 转换为可访问的 URL
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/styles/style-schema.ts:
```typescript
export type CreateUserStyleInput = z.infer<typeof createUserStyleSchema>
export type UpdateUserStyleInput = z.infer<typeof updateUserStyleSchema>
```

From src/lib/styles/style-service.ts:
```typescript
export const MAX_STYLE_LIMIT = 20
```

From src/hooks/useUserStyles.ts:
```typescript
export interface UserStyle {
  id: string
  name: string
  promptZh: string
  promptEn: string
  tags: string | null
  referenceImageUrl: string | null
  createdAt: Date
  updatedAt: Date
}
```

From src/contexts/ToastContext.tsx:
```typescript
export function useToast(): ToastContextValue
```

From src/lib/storage/index.ts:
```typescript
export function toFetchableUrl(inputUrl: string): string
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 StyleTagSelector 组件</name>
  <files>
    src/components/styles/StyleTagSelector.tsx
  </files>
  <read_first>
    src/lib/styles/style-service.ts
  </read_first>
  <action>
    创建 src/components/styles/StyleTagSelector.tsx 组件。

    功能要求：
    1. 支持预设标签快速选择（chip 模式）
    2. 支持自定义标签输入
    3. 已选标签高亮显示

    Props 接口：
    ```typescript
    interface StyleTagSelectorProps {
      value: string[]  // 已选标签
      onChange: (tags: string[]) => void
    }
    ```

    组件实现：
    ```tsx
    'use client'

    import { useState } from 'react'
    import { AppIcon } from '@/components/ui/icons'

    // 预设标签列表
    const PRESET_TAGS = ['写实', '动漫', '抽象', '油画', '水彩', '赛璐璐', '厚涂', '像素']

    interface StyleTagSelectorProps {
      value: string[]
      onChange: (tags: string[]) => void
    }

    export function StyleTagSelector({ value, onChange }: StyleTagSelectorProps) {
      const [customInput, setCustomInput] = useState('')

      const toggleTag = (tag: string) => {
        if (value.includes(tag)) {
          onChange(value.filter(t => t !== tag))
        } else {
          onChange([...value, tag])
        }
      }

      const handleAddCustom = () => {
        const trimmed = customInput.trim()
        if (trimmed && !value.includes(trimmed)) {
          onChange([...value, trimmed])
          setCustomInput('')
        }
      }

      const handleKeyDown = (e: React.KeyboardEvent) => {
        if (e.key === 'Enter') {
          e.preventDefault()
          handleAddCustom()
        }
      }

      return (
        <div className="space-y-3">
          <label className="block text-sm font-medium text-[var(--glass-text-primary)]">
            标签
          </label>

          {/* 预设标签 */}
          <div className="flex flex-wrap gap-2">
            {PRESET_TAGS.map(tag => (
              <button
                key={tag}
                type="button"
                onClick={() => toggleTag(tag)}
                className={`px-3 py-1.5 rounded-full text-sm border transition-all cursor-pointer ${
                  value.includes(tag)
                    ? 'border-[var(--glass-accent-from)] bg-[var(--glass-accent-from)]/10 text-[var(--glass-accent-from)]'
                    : 'border-[var(--glass-stroke-soft)] text-[var(--glass-text-secondary)] hover:border-[var(--glass-stroke-base)]'
                }`}
              >
                {tag}
              </button>
            ))}
          </div>

          {/* 已选标签展示 */}
          {value.length > 0 && (
            <div className="flex flex-wrap gap-1.5">
              {value.map(tag => (
                <span
                  key={tag}
                  className="inline-flex items-center gap-1 px-2 py-1 rounded-md bg-[var(--glass-bg-muted)] text-xs text-[var(--glass-text-secondary)]"
                >
                  {tag}
                  <button
                    type="button"
                    onClick={() => onChange(value.filter(t => t !== tag))}
                    className="hover:text-[var(--glass-text-primary)]"
                  >
                    <AppIcon name="close" className="w-3 h-3" />
                  </button>
                </span>
              ))}
            </div>
          )}

          {/* 自定义标签输入 */}
          <div className="flex gap-2">
            <input
              type="text"
              value={customInput}
              onChange={(e) => setCustomInput(e.target.value)}
              onKeyDown={handleKeyDown}
              placeholder="添加自定义标签..."
              className="glass-input-base h-9 px-3 text-sm flex-1"
            />
            <button
              type="button"
              onClick={handleAddCustom}
              disabled={!customInput.trim()}
              className="glass-btn-base glass-btn-secondary px-3 py-1.5 text-sm disabled:opacity-50"
            >
              添加
            </button>
          </div>
        </div>
      )
    }
    ```
  </action>
  <verify>
    <automated>grep -n "StyleTagSelector" src/components/styles/StyleTagSelector.tsx && grep -n "PRESET_TAGS" src/components/styles/StyleTagSelector.tsx</automated>
  </verify>
  <done>
    - StyleTagSelector 组件已创建
    - 支持 8 个预设标签选择
    - 支持自定义标签输入
    - 已选标签可移除
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 ReferenceImageUpload 组件</name>
  <files>
    src/components/styles/ReferenceImageUpload.tsx
  </files>
  <read_first>
    src/lib/storage/index.ts
    src/lib/api-fetch.ts
  </read_first>
  <action>
    创建 src/components/styles/ReferenceImageUpload.tsx 组件。

    功能要求：
    1. 支持图片文件选择和上传
    2. 显示当前参考图预览
    3. 显示 AI 提取状态（pending/completed/failed）
    4. 提取失败时显示重试按钮

    Props 接口：
    ```typescript
    interface ReferenceImageUploadProps {
      styleId: string | null  // 编辑时有值，创建时为 null
      referenceImageUrl: string | null
      extractionStatus: 'pending' | 'completed' | 'failed'
      extractionMessage: string | null
      onUploadSuccess: (data: { referenceImageUrl: string; extractionStatus: string }) => void
      onStatusChange: (status: 'pending' | 'completed' | 'failed', message?: string) => void
    }
    ```

    组件实现要点：
    1. 使用 useRef 管理隐藏的 file input
    2. 图片预览使用 toFetchableUrl 转换 URL
    3. 状态角标使用不同颜色：
       - pending: 蓝色背景，显示"AI 提取中..."
       - completed: 绿色背景，显示"提取完成"
       - failed: 红色背景，显示"提取失败"
    4. 上传逻辑：
       - 仅在 styleId 存在时才实际上传
       - 使用 FormData 封装文件
       - POST 到 /api/user-styles/:id/upload-ref
    5. 提取状态轮询（在 StyleCreateModal 中实现）

    样式参考：
    - 图片预览容器: `relative w-full aspect-video rounded-lg overflow-hidden bg-[var(--glass-bg-muted)]`
    - 状态角标: `absolute top-2 right-2 px-2 py-1 rounded text-xs text-white`
    - 上传按钮: `glass-btn-base glass-btn-secondary`
  </action>
  <verify>
    <automated>grep -n "ReferenceImageUploadProps" src/components/styles/ReferenceImageUpload.tsx && grep -n "toFetchableUrl" src/components/styles/ReferenceImageUpload.tsx</automated>
  </verify>
  <done>
    - ReferenceImageUpload 组件已创建
    - 支持图片选择和上传
    - 显示图片预览
    - 显示 AI 提取状态
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 创建 StyleCreateModal 组件</name>
  <files>
    src/components/styles/StyleCreateModal.tsx
    messages/zh/profile.json
  </files>
  <read_first>
    src/components/ui/primitives/GlassModalShell.tsx
    src/lib/styles/style-schema.ts
    src/lib/api-fetch.ts
    src/components/styles/StyleTagSelector.tsx
    src/components/styles/ReferenceImageUpload.tsx
    src/hooks/useUserStyles.ts
    src/contexts/ToastContext.tsx
  </read_first>
  <action>
    创建 src/components/styles/StyleCreateModal.tsx 组件。

    Props 接口：
    ```typescript
    interface StyleCreateModalProps {
      isOpen: boolean
      onClose: () => void
      onSuccess: () => void  // 创建/编辑成功后回调
      editStyle?: UserStyle | null  // 编辑模式时传入
    }
    ```

    组件结构：
    1. 使用 GlassModalShell 作为弹窗壳
    2. 表单字段：
       - 风格名称（必填，1-50字符）
       - 中文提示词（必填，1-2000字符）
       - 英文提示词（必填，1-2000字符）
       - 标签选择器（StyleTagSelector）
       - 参考图上传（ReferenceImageUpload）
    3. 底部按钮：取消、保存

    状态管理：
    ```typescript
    const [formData, setFormData] = useState({
      name: '',
      promptZh: '',
      promptEn: '',
      tags: [] as string[],
    })
    const [referenceImageUrl, setReferenceImageUrl] = useState<string | null>(null)
    const [extractionStatus, setExtractionStatus] = useState<'pending' | 'completed' | 'failed'>('pending')
    const [extractionMessage, setExtractionMessage] = useState<string | null>(null)
    const [isSubmitting, setIsSubmitting] = useState(false)
    const [createdStyleId, setCreatedStyleId] = useState<string | null>(null)
    ```

    创建流程：
    1. 用户填写表单
    2. 点击保存
    3. POST /api/user-styles 创建风格
    4. 获取返回的 style.id
    5. 如果有参考图，上传参考图
    6. 开始轮询提取状态

    编辑流程：
    1. 根据 editStyle 初始化表单数据
    2. 点击保存
    3. PUT /api/user-styles/:id 更新风格
    4. 如果有新参考图，上传参考图

    表单验证：
    - 使用前端验证（与 Zod schema 一致）
    - 名称、提示词为必填项

    提取状态轮询：
    - 当 extractionStatus === 'pending' 时，每 3 秒轮询一次
    - GET /api/user-styles/:id 获取最新状态
    - 状态变为 completed 或 failed 时停止轮询
    - completed 时自动填充 promptZh 和 promptEn（如果为空）

    文案添加到 messages/zh/profile.json：
    ```json
    "styleName": "风格名称",
    "styleNamePlaceholder": "输入风格名称",
    "promptZh": "中文提示词",
    "promptZhPlaceholder": "描述风格的视觉特征",
    "promptEn": "英文提示词",
    "promptEnPlaceholder": "Describe the visual style characteristics",
    "referenceImage": "参考图",
    "uploadReference": "上传参考图",
    "changeReference": "更换参考图",
    "extracting": "AI 提取中...",
    "extractCompleted": "提取完成",
    "extractFailed": "提取失败",
    "retryExtract": "重试",
    "styleCreated": "风格创建成功",
    "styleUpdated": "风格更新成功",
    "save": "保存",
    "cancel": "取消"
    ```
  </action>
  <verify>
    <automated>grep -n "StyleCreateModalProps" src/components/styles/StyleCreateModal.tsx && grep -n "GlassModalShell" src/components/styles/StyleCreateModal.tsx</automated>
  </verify>
  <done>
    - StyleCreateModal 组件已创建
    - 支持创建和编辑两种模式
    - 表单验证完整
    - 集成标签选择器
    - 集成参考图上传
    - 实现提取状态轮询
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: 集成 StyleCreateModal 到 StyleManager</name>
  <files>
    src/components/styles/StyleManager.tsx
    messages/zh/profile.json
  </files>
  <read_first>
    src/components/styles/StyleManager.tsx
    src/components/styles/StyleCreateModal.tsx
  </read_first>
  <action>
    修改 StyleManager 组件，集成 StyleCreateModal。

    1. 添加状态：
    ```typescript
    const [isCreateModalOpen, setIsCreateModalOpen] = useState(false)
    const [editTarget, setEditTarget] = useState<UserStyle | null>(null)
    ```

    2. 导入 StyleCreateModal：
    ```typescript
    import { StyleCreateModal } from './StyleCreateModal'
    ```

    3. 修改新建按钮点击处理：
    ```typescript
    onClick={() => {
      setEditTarget(null)
      setIsCreateModalOpen(true)
    }}
    ```

    4. 修改 StyleCard 的 onEdit 回调：
    ```typescript
    onEdit={(id) => {
      const style = styles.find(s => s.id === id)
      if (style) {
        setEditTarget(style)
        setIsCreateModalOpen(true)
      }
    }}
    ```

    5. 添加 StyleCreateModal 渲染：
    ```tsx
    <StyleCreateModal
      isOpen={isCreateModalOpen}
      onClose={() => {
        setIsCreateModalOpen(false)
        setEditTarget(null)
      }}
      onSuccess={() => {
        setIsCreateModalOpen(false)
        setEditTarget(null)
        refresh()  // 刷新列表
      }}
      editStyle={editTarget}
    />
    ```

    6. 确保使用 refresh 方法刷新列表（来自 Task 5 扩展的 useUserStyles）
  </action>
  <verify>
    <automated>grep -n "StyleCreateModal" src/components/styles/StyleManager.tsx && grep -n "isCreateModalOpen" src/components/styles/StyleManager.tsx</automated>
  </verify>
  <done>
    - StyleManager 集成 StyleCreateModal
    - 新建按钮可打开创建弹窗
    - 编辑按钮可打开编辑弹窗（预填充数据）
    - 创建/编辑成功后刷新列表
  </done>
</task>

</tasks>

<verification>
1. 点击"新建风格"按钮打开创建弹窗
2. 弹窗包含名称、中文提示词、英文提示词、标签选择器、参考图上传
3. 填写必填项后可保存风格
4. 上传参考图后显示 AI 提取状态
5. AI 提取完成后提示词自动填充
6. 点击风格卡片的编辑按钮可打开编辑弹窗（预填充数据）
7. 编辑后保存可更新风格
8. 所有操作有 Toast 反馈
</verification>

<success_criteria>
- 用户可以点击"新建风格"，填写名称、提示词和标签后保存
- 用户可以在创建/编辑弹窗中上传参考图，等待 AI 提取草稿，编辑草稿后保存
- 用户可以编辑已有风格（名称、提示词、标签）
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui/09-02-SUMMARY.md`
</output>
