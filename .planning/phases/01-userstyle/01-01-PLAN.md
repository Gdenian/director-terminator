---
phase: 01-userstyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - tests/integration/user-style.test.ts
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "prisma migrate dev 成功运行，UserStyle 表在数据库中存在"
    - "UserStyle 记录可以关联到 User，包含所有必要字段"
    - "Prisma Client 重新生成后 TypeScript 中 prisma.userStyle 可用"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "UserStyle 模型定义和 User 反向关系"
      contains: "model UserStyle"
    - path: "tests/integration/user-style.test.ts"
      provides: "DATA-01 集成测试"
      exports: []
  key_links:
    - from: "prisma/schema.prisma UserStyle.userId"
      to: "prisma/schema.prisma User.id"
      via: "@relation(fields: [userId], references: [id], onDelete: Cascade)"
      pattern: "relation.*userId.*references.*id"
    - from: "prisma/schema.prisma User"
      to: "prisma/schema.prisma UserStyle"
      via: "styles UserStyle[] 反向关系字段"
      pattern: "styles\\s+UserStyle\\[\\]"
---

<objective>
向 prisma/schema.prisma 新增 UserStyle 模型，更新 User 模型添加反向关系字段，执行数据库迁移，重新生成 Prisma Client，并创建集成测试验证表结构。

Purpose: 建立整个自定义风格系统的数据基础——没有此步骤，Phase 2 的解析器和后续所有 API 均无法运行。
Output: user_styles 表在 MySQL 中存在，@prisma/client 包含 UserStyle 类型。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-userstyle/01-CONTEXT.md
@.planning/phases/01-userstyle/01-RESEARCH.md
</context>

<interfaces>
<!-- 现有 schema.prisma 中的关键模式，执行者直接参考，无需重新探索 -->

User 模型末尾当前状态（须在 graphEvents 行后、@@map 前插入反向关系字段）：
```prisma
  graphEvents        GraphEvent[]

  @@map("user")
}
```

项目 JSON 字段约定（使用 String? @db.Text，不用 @db.Json）：
```prisma
// 来自 VideoEditorProject
projectData  String?  @db.Text
// 来自 NovelPromotionPanel
actingNotes  String?  @db.Text
```

完整的 UserStyle 模型（照此实现，不要偏离）：
```prisma
model UserStyle {
  id                String   @id @default(uuid())
  userId            String
  name              String
  promptZh          String   @db.Text
  promptEn          String   @db.Text
  tags              String?  @db.Text
  referenceImageUrl String?  @db.Text
  isSystem          Boolean  @default(false)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @default(now()) @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, name])
  @@index([userId])
  @@map("user_styles")
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: 创建集成测试骨架</name>
  <files>tests/integration/user-style.test.ts</files>
  <read_first>
    - tests/integration/ 目录（了解现有集成测试的组织方式和引入模式）
    - prisma/schema.prisma（了解现有模型，验证测试期望的字段名）
  </read_first>
  <behavior>
    - 测试 1: 创建 UserStyle 记录后可通过 prisma.userStyle.findUnique 查询到
    - 测试 2: UserStyle 记录包含正确字段：id (uuid 格式), name, promptZh, promptEn, isSystem (默认 false), createdAt, updatedAt
    - 测试 3: tags 和 referenceImageUrl 为可选字段，不传时为 null
    - 测试 4: userId + name 联合唯一约束生效——相同 userId + name 二次创建抛出唯一约束错误
    - 测试 5: 删除关联 User 时，UserStyle 记录级联删除（onDelete: Cascade 生效）
  </behavior>
  <action>
    在 tests/integration/ 目录下创建 user-style.test.ts。

    文件结构：
    - 引入 PrismaClient（与现有集成测试相同的引入方式）
    - beforeEach/afterEach 清理测试数据，确保测试隔离
    - 注意：此文件在 Task 1（迁移完成）之前运行会失败，这是预期行为（RED 阶段）
    - 使用中文注释说明每个测试的验证目的

    测试数据构造：先创建测试 User（email: 'test-style@example.com'），再通过 userId 创建 UserStyle。

    测试 4 的唯一约束验证：捕获 Prisma 错误码 P2002（唯一约束冲突）。

    注意：迁移执行前此文件无法通过，Task 1 执行后方可运行。
  </action>
  <verify>
    <automated>MISSING — Task 1 必须先执行数据库迁移，迁移完成后运行: npx vitest run tests/integration/user-style.test.ts</automated>
  </verify>
  <done>tests/integration/user-style.test.ts 文件存在，包含 5 个测试用例，每个测试有中文注释说明验证意图</done>
</task>

<task type="auto">
  <name>Task 1: 更新 Prisma Schema 并执行迁移</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma（必读全文——理解现有模型位置，找到 User 模型的 graphEvents 行和 @@map("user") 位置）
  </read_first>
  <action>
    执行两处改动：

    **改动 1：在 User 模型中添加反向关系字段**

    找到 User 模型中的 `graphEvents  GraphEvent[]` 行，在其后（`@@map("user")` 前）插入：
    ```
      styles             UserStyle[]
    ```

    **改动 2：在 schema.prisma 末尾（最后一个模型之后）添加 UserStyle 模型**

    按照 <interfaces> 中提供的完整模型定义逐字添加，注意：
    - tags 字段类型必须是 `String?  @db.Text`（不是 `@db.Json`）——遵循项目 JSON 字段约定（D-02）
    - referenceImageUrl 同样使用 `String?  @db.Text`
    - 表名映射 `@@map("user_styles")`
    - onDelete: Cascade（与项目其他用户关联模型一致）
    - 联合唯一索引 `@@unique([userId, name])` 实现 D-03
    - 单字段索引 `@@index([userId])` 实现 D-04

    **改动完成后执行**：
    ```bash
    npx prisma validate
    npx prisma migrate dev --name add_user_style
    npx prisma generate
    ```

    如果 migrate dev 要求交互输入，使用 `--create-only` 先生成 SQL 文件，再用 `npx prisma migrate deploy` 应用。

    迁移成功后验证 TypeScript 编译：
    ```bash
    npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>npx prisma validate && npx tsc --noEmit && npx vitest run tests/integration/user-style.test.ts</automated>
  </verify>
  <done>
    - prisma/schema.prisma 包含 model UserStyle { 定义
    - User 模型包含 styles UserStyle[] 字段
    - prisma/migrations/ 下存在名称含 add_user_style 的迁移目录
    - npx tsc --noEmit 无报错（prisma.userStyle 类型可用）
    - 5 个集成测试全部通过
  </done>
</task>

</tasks>

<verification>
全部验证通过信号：
1. `npx prisma validate` 返回 "The schema at prisma/schema.prisma is valid"
2. `npx prisma migrate status` 显示无待应用迁移
3. `npx tsc --noEmit` 无 TypeScript 错误
4. `npx vitest run tests/integration/user-style.test.ts` 5 个测试全部通过
5. schema.prisma 中 `tags String? @db.Text`（不是 @db.Json）
</verification>

<success_criteria>
- UserStyle 表在 MySQL 数据库中存在，含全部 11 个字段
- prisma.userStyle.create / findUnique / delete 在 TypeScript 中类型正确可用
- userId + name 联合唯一约束在数据库层生效
- User 删除时 UserStyle 级联删除
</success_criteria>

<output>
完成后创建 `.planning/phases/01-userstyle/01-userstyle-01-SUMMARY.md`
</output>
